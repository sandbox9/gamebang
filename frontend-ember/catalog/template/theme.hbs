<h2>Catalog 페이지입니다.</h2>
{{partial "nav"}}

<!--TODO 별도 파일로 빼내기 -->
<h3>상품리스트</h3>
<section id="content">
    <div id="productList">
        {{#each product in model.productList}}
        <div>
            {{#link-to 'product' product.productId}}
                <img {{bind-attr src=product.imageList.[0].imageUrl }}
                     style="height: 200px;" />
            {{/link-to}}
            {{#link-to 'product' product.productId}}
                {{product.productName}} <br />
                {{product.salePrice}} 원
            {{/link-to}}
        </div>
        {{/each}}
    </div>
</section>